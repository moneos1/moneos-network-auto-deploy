{
  "name": "moneos-network-auto-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:bscTestnet": "hardhat run scripts/deploy.js --network bscTestnet"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.1",
    "hardhat": "^2.22.10"
  }
}